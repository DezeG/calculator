It's just simple line calculator made to practice coding

Possible operations: '+', '-', '*', '/', '^'

Basicly the program translates the expression into reverse polish notation at fisrt.
Then it solves it
